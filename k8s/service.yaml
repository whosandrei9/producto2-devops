apiVersion: v1
kind: Service
metadata:
  name: producto2-uoc
  namespace: __NAMESPACE__
spec:
  type: NodePort
  selector:
    app: producto2-uoc
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
